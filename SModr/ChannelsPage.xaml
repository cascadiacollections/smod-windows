<Page
    x:Class="SModr.ChannelsPage"
    x:Name="Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:media="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:local="using:SModr"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="TextTemplate">
            <StackPanel Width="240" Orientation="Horizontal">
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Margin="8,0,0,0" Text="{Binding Path=Url}" />
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <GridView
        x:Name="ContentGridView"
        IsItemClickEnabled="True"
        CanDragItems="False"
        AllowDrop="False"
        CanReorderItems="False"
        SelectionMode="None"
        ItemClick="ContentGridView_ItemClick"
        FlowDirection="LeftToRight">

        <GridView.ItemTemplate>
            <DataTemplate x:DataType="local:Channel">
                <TextBlock Text="{x:Bind Url}"/>
            </DataTemplate>
        </GridView.ItemTemplate>
    </GridView>
</Page>
